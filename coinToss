import random

def coinToss(start, stop) :
    totalPlays = " "
    for i in range(start, stop) :
        num = random.randint(0,1)
        totalPlays = totalPlays + str(num) + " "
    print(totalPlays)

coinToss(0,11)
